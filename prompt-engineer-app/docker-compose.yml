version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "3000:3000"
    volumes:
      - ./database:/app/database
      - ./contexts:/app/contexts
    environment:
      - BACKEND_PORT=8000
      - FRONTEND_PORT=3000
    restart: unless-stopped
